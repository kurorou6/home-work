# read length(длина)
length = float(input("Enter length: "))
# read breadth(Широта)
breadth = float(input("Enter breadth: "))
# calculate area(область.площадь)
area = length * breadth
# display result
print("Area of rectangle is:", area)